import React from 'react';

function IBM() {
  const ibmStyle = {
    backgroundColor: 'blue',
    width: '100%',
    height: '100vh',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    color: 'white', 
  };

  return (
    <div style={ibmStyle}>
      IBM
    </div>
  );
}

export default IBM;
